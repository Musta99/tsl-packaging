import 'package:flutter/material.dart';

class DeletedDataScreen extends StatelessWidget {
  const DeletedDataScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Deleted Data"),
      ),
    );
  }
}
